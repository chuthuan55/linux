# install docker

# install docker-compose

# add new port in file docker-compose.yml

# use php 74

# upload file webserver in winscp from window to linux

cd /root/webser/

docker-compose up -d

# check file:
  docker-compose ps

# open port by firewall

# create file info.php

# go to the web name: ipaddress:port/info.php

# add container "mysql"
  
chmod 777 -R /var/www/
